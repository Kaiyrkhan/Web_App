
```shell
$ sudo apt update
$ sudo apt install -y vim mosh tmux htop git curl wget unzip zip build-essential make
```

```shell
$ sudo nano /etc/ssh/ssh_config
  AllowUsers student
  PermitRootLogin no
  PasswordAuthentication no

$ sudo systemctl restart ssh
```

```shell
```

```shell
```

```shell
```

### References

[Поднимаем Debian сервер для PythonDjango](https://vkvideo.ru/video-206723877_456239074)
